Utilizando la clase Operaciones, realizar la operación asignada.
De igual manera deberás realizar la prueba correspondiente en el archivo main.py

Nombre              |   Operación
-----------------------------------------------------------------------------
Alejandra           | maximo(lista) -> Regresa el número mayor de la lista
Juan Pablo          | minimo(lista) -> Regresa el número menor de la lista
Diego Flores        | promedio(lista) -> Regresa el promedio de los números de la lista
Felix Alejandro     | ordenarLista(lista) -> Regresa la lista ordenada
Jesús Elián         | eliminarDuplicados(lista) -> Regresa la lista sin números duplicados
Francisco Emiliano  | esPar(numero) -> Regresar True si el número es par False en caso contrario
Luis Enrique        | esImpar(numero) -> Regresa True si el número es impar False en caso contrario
Emiliano            | numeroMayo(a,b,c) -> Regresa el número mayor de los tres números recibidos 
Emiliano            | generarTablaMultiplicar(numero) -> Imprime la tabla de multiplicar del número indicado
Javier Alejandro    | contarPalabraEnTexto(texto,palabra) -> Regresa el número de veces que se encuentra la palabra en el texto
Bruno Gael          | serieFibonacci(n) -> Imprime la serie fibonacci hasta el número n
Julio Alfonso       | reemplazarEspacios(texto, caracter) -> Regresa una cadena con el texto reemplando los espacios con el caracter
Lizzet              | contarVocales(texto) -> Cuenta y regresa el número de vocales que tiene el texto